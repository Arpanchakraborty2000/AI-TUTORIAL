# 🚀 GenAI + Agentic AI Environment Setup (Conda)

This guide walks you through setting up a **production-ready GenAI + Agentic AI environment** using **Conda (Python 3.11)** on macOS / Linux / Windows.

---

## 🧠 Prerequisites

Make sure you have:
- Conda installed (Miniconda / Anaconda)
- Terminal access
- Internet connection

Verify conda:
```bash
conda --version
```

---

## 1️⃣ Create a GenAI Conda Environment (Python 3.11)

```bash
conda create -n genai python=3.11
```

---

## 2️⃣ Activate the Environment

```bash
conda activate genai
```

You should now see:
```text
(genai) arpan@ARPANs-MacBook-Air ~ %
```

---

## 3️⃣ Install Jupyter (inside `genai` environment)

```bash
conda install -c conda-forge jupyterlab ipykernel
```

### Register Jupyter Kernel
```bash
python -m ipykernel install --user --name genai --display-name "GenAI (conda)"
```

Launch Jupyter:
```bash
jupyter lab
```

---

## 4️⃣ Install Agentic AI + GenAI Stack (using pip)

> ⚠️ `pip` is recommended here because LangChain & Hugging Face update faster via pip.

```bash
pip install \
  langchain \
  langgraph \
  langchain-community \
  langchain-openai \
  transformers \
  accelerate \
  sentencepiece \
  huggingface-hub
```

---

## 5️⃣ Install ML + Vector Database Libraries

```bash
pip install \
  torch \
  scikit-learn \
  pandas \
  numpy \
  matplotlib \
  faiss-cpu \
  chromadb
```

---

## 6️⃣ Quick Verification Test

```bash
python - <<EOF
import torch, transformers, langchain, langgraph
print("GenAI + Agentic AI setup READY 🚀")
EOF
```

If you see the success message — your setup is complete ✅

---

## 🔒 Best Practices

- ❌ Never install project packages in `(base)`
- ✅ One Conda environment per project
- ✅ Use Python **3.10 or 3.11** for GenAI / ML
- ✅ Prefer `pip` for GenAI libraries

---

## 🧠 You Are Now Ready For

- LangChain pipelines
- LangGraph multi-agent workflows
- Hugging Face & Transformers models
- Vector DB–powered RAG systems
- Production-ready GenAI projects

---

Happy building 🚀
