# 📦 Creating a Python Package Using Conda

This guide explains **step-by-step** how to create a **Python package using Conda**, suitable for **ML, GenAI, and Agentic AI projects**.

---

## 🧠 Prerequisites

Make sure you have:
- Conda installed (Miniconda or Anaconda)
- macOS / Linux / Windows
- Basic terminal knowledge

Verify conda:
```bash
conda --version
```

---

## 🐍 Step 1: Create a Conda Environment

Never work in the base environment.

```bash
conda create -n mypackage python=3.11
conda activate mypackage
```

You should see:
```text
(mypackage) $
```

---

## 📁 Step 2: Create Project Structure

```bash
mkdir my-python-package
cd my-python-package
```

Recommended structure:
```text
my-python-package/
├── src/
│   └── mypackage/
│       ├── __init__.py
│       └── main.py
├── README.md
├── pyproject.toml
└── requirements.txt
```

---

## 📦 Step 3: Create the Python Package

```bash
mkdir -p src/mypackage
touch src/mypackage/__init__.py
touch src/mypackage/main.py
```

Now `mypackage` is a valid Python package ✅

Example `main.py`:
```python
def hello():
    print("Hello from my Conda package!")
```

---

## 🧾 Step 4: Create `pyproject.toml`

```toml
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "mypackage"
version = "0.1.0"
description = "My first Conda-based Python package"
authors = [{ name="Your Name" }]
dependencies = []
```

---

## 📥 Step 5: Install Package in Editable Mode

```bash
pip install -e .
```

Test it:
```bash
python - <<EOF
from mypackage.main import hello
hello()
EOF
```

---

## 📌 Step 6: Add Dependencies (Optional)

```bash
pip install numpy pandas
pip freeze > requirements.txt
```

---

## 🧪 Step 7: Verify Environment

```bash
conda list
```

---

## 🔒 Best Practices

- ❌ Don’t install packages in `(base)`
- ✅ One conda env per project
- ✅ Keep source code inside `src/`
- ✅ Use Python 3.10 or 3.11 for ML/GenAI

---

## 🚀 Ready For

- ML projects
- GenAI / Agentic AI
- LangChain / Hugging Face
- Production-ready packaging

---

Happy coding 🚀
